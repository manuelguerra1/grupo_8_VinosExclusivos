<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>

    

    <script src="https://kit.fontawesome.com/b5956fc21b.js" crossorigin="anonymous"></script>
    <script src="https://kit.fontawesome.com/d4cb0f35d2.js" crossorigin="anonymous"></script>
    <link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="../css/partials.css">
    <link rel="stylesheet" href="../css/productCreateForm.css">
    <title>Login</title>
</head>
<body>
<header>
    <div class="top-header">

        <a href="/">
            <div class="logo-to-menu"> <img class="header-logo" src="../img/icons/logo-sin-fondo-1-beige.png" alt="logo" > </a>
        </a>
</header>
<main>
    <form class="prod-create-form" method="post" action = "/save" enctype="multipart/form-data">
        <ul class="errores">
            
        </ul>
        <div class="registroProducto-prod-create-form">
            
            <h3> CREACIÓN DE PRODUCTOS</h3>

            <label for="nameproduct"></label>
            <h4>Nombre del Producto: </h4><input type="text" name="name" id="name" placeholder="Nombre del Producto" value="<%= locals.old && old.name %>">
            <% if (locals.errors && errors.name) { %>
            <p class="feedback"><%= errors.name %></p>
            <% } %>
            <br>

            <label for="description"></label>
            <h4>Descripción: </h4><input type="text" name="description" id="description" placeholder="Descripción" value="<%= locals.old && old.description %>">
            <% if (locals.errors && errors.description) { %>
                <p class="feedback"><%= errors.description %></p>
                <% } %>
            <br>

            <label for="price"></label>
            <h4>Precio: </h4>
            <input type="number" name="price" id="price" placeholder="Precio"><p class="p"></p>
            <br>

            <label for="year"></label>
            <h4>Año de Cosecha: </h4><input type="text" name="year" id="year" placeholder="Año de Cosecha" ><p class="p"></p>
            <br>
            
            
            <label for="varietal"><h4>Variedad: </h4></label>
            <select class="select-prod-create" name="varietal" id="varietal" >
                <option value="">Seleccione una variedad:</option>
                
                <% varietal.forEach(element => { %>
                    <option value="<%= element.id %>"><%= element.varietal_name %></option>
                <% }) %>
                
            </select><p class="p"></p>

            <br>

            <label for="brand"><h4>Marca: </h4></label>
            <select class="select-prod-create" name="brand" id="brand" >
                <option value="">Seleccione una marca:</option>
                <% brand.forEach(element => { %>
                    <option value="<%= element.id %>"><%= element.brand_name %></option>
                <% }) %>
            </select><p class="p"></p>
            
            <br>
            
            <label for="origen"> <h4>País de Origen:</h4></label>
                <select class="select-prod-create" name="origen" id="origen" >
                    <option value="">Seleccione origen:</option>
                    <% origin.forEach(element => { %>
                        <option value="<%= element.id %>"><%= element.country %></option>
                    <% }) %>
                </select><p class="p"></p>
            </br>
            
            <label for="region"><h4>Región: </h4></label>
            <select class="select-prod-create" name="region" id="region" >
                <option value="">Seleccione una región:</option>
                <% region.forEach(element => { %>
                    <option value="<%= element.id %>"><%= element.region %></option>
                <% }) %>
            </select><p class="p"></p>

            <br>
            
            <label for="category"> <h4>Categorías</h4></label>
                <select class="select-prod-create" name="category" id="category" >
                    <option value="">Seleccione una categoría:</option>
                    <% category.forEach(element => { %>
                        <option value="<%= element.id %>"><%= element.category_name %></option>
                    <% }) %>
                </select><p class="p"></p>
            </br>
            
            <label for="image"></label>
            <h4 class="prod-img-prod-create-form">Imágen de producto</h4><input type="file" name="image" id="image" value="<%= locals.old && old.image %>">
            <% if (locals.errors && errors.image) { %>
                <p class="feedback"><%= errors.image %></p>
                <% } %>
            
            <br>
            <div class="prodCreate-buttons-prod-create-form">
                <button type="submit" value="Crear">CREAR</button>
                
            </div>

            
        </div>
    </form>
</main>
    <!-- <script src="/js/prodCreate.js" type="text/javascript"></script>
    <script src="/js/createFunctions.js" type="text/javascript"></script> -->
</body>
<br>
<footer>
    <div class="grupo2">
    <small>&copy; 2023 <b>Foo Drinks.Co</b> - Todos los derechos reservados</small>
</div>
</footer>